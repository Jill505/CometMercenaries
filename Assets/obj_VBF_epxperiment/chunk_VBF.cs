using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class chunk_VBF : MonoBehaviour
{
    public int chunkX = 0;
    public int chunkY = 0;

    public bool isEntry;
    public int entrySort = 0;//強制規定：chunk必須為
    public int entrySubSort = 0;// 0 1 2

    public string chunkType;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
